apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: forumservice-db-data
  name: forumservice-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
