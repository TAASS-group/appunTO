apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: userservice-db-data
  name: userservice-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
