apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: courseservice-db-data
  name: courseservice-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
